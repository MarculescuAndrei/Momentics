import { View, Text } from "react-native";
import React from "react";

const EmailVerificationScreen = () => {
  return (
    <View>
      <Text>Your Email isn't Verified</Text>
    </View>
  );
};

export default EmailVerificationScreen;
